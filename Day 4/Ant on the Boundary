class Solution(object):
    def returnToBoundaryCount(self, nums):
        count=0
        step=0
        for i in nums:
            count+=i
            if count==0:
                step+=1
        return step
        

       
        
