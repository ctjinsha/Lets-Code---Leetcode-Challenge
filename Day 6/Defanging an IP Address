class Solution:
    def defangIPaddr(self, address: str) -> str:
        sr=""
        for i in address:
            if i=='.':
                sr=sr+"[.]"
            else :
                sr=sr+i
        return sr
        
