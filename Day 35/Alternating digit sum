class Solution:
    def alternateDigitSum(self, n: int) -> int:
        sum=0
        sign=1
        for i in str(n):
            digit=int(i)
            sum+=digit*sign
            sign*=-1
        return sum
       


            



        
        
