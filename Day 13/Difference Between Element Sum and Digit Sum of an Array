class Solution:
    def differenceOfSum(self, nums: List[int]) -> int:
        esum=0
        dsum=0
        for i in nums:
            esum+=i
            while i>0:
                digit=i%10
                dsum+=digit
                i//=10
        return esum-dsum

            
